import { NumerosdecimalesDirective } from './numerosdecimales.directive';

describe('NumerosdecimalesDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosdecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
